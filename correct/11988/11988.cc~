#include <iostream>
#include <list>
#include <string>
using namespace std;

int main() {
   string line;
   while(getline(cin, line)) {
      list<char> a(100000);
      for(int i = 0; i < line.size(); ++i) {

      }
   }
   return 0;
}
