#include<iostream>
using namespace std;

int main() {
   int cases;
   cin >> cases;
   for(int i = 0; i < cases; ++i) {
      // total is n!
      // number of ways for EVERYONE to take the wrong hat?
      int n;
      int total = 1;
      for(int i = 2; i <= n; ++i) {

      }
   }
   
   return 0;
}
