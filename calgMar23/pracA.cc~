#include <iostream>

int main() {

   int T;
   cin >> T;
   for(int i = 0; i < T; i++) {
      int a, b;
      cin >> a >> b;
      int ans = 1;
      for(int j = 0; j < b; i++) {
	 ans *= a;
      }
      cout << ans % 10 << endl;
   }
   
   return 0;
}
