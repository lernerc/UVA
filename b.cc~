#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
   int n;
   while (cin >> n && n!=0)
   {
      vector<int> x, y;
      for(int i=0;i<n;i++)
      {
	 int q,r;
	 cin >> q >> r;
	 x.push_back(q);
	 y.push_back(r);
      }
   }
   return 0;
}
